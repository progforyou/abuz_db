/*
const { Scenes } = require('telegraf');
const getUser = new Scenes.WizardScene('getUser',
    (ctx) => {
        ctx.reply('Enter name here');
        return ctx.wizard.next();
    },
    (ctx) => {
        // validation example
        ctx.reply(`I'm searching ${ctx.message}`, {});
        return ctx.wizard.next();
    },
    );


module.exports = getUser*/
